# Variables para docker-compose
# Copia a .env y completa los valores

# PostgreSQL
DB_PASSWORD=claveo_dev_password_change_in_production

# JWT (genera con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=change-me-super-secret-jwt-key-minimum-32-characters-here
JWT_REFRESH_SECRET=change-me-refresh-secret-different-from-jwt-secret-here

# Entorno
NODE_ENV=development

# CORS
ALLOWED_ORIGINS=http://localhost:3000
